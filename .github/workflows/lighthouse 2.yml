# Lighthouse performance baseline — DISABLED by default.
# Enable by uncommenting the jobs and adapting the URL/build steps
# (see and3rn3t/homehub and and3rn3t/health for working examples).

name: Lighthouse

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Enable Lighthouse by adapting this workflow — see homehub/health repos."
# jobs:
#   lighthouse:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v6
#       - uses: actions/setup-node@v6
#         with: { node-version-file: .nvmrc, cache: npm }
#       - run: npm ci && npm run build
#       - uses: treosh/lighthouse-ci-action@v12
#         with:
#           uploadArtifacts: true
#           temporaryPublicStorage: true
